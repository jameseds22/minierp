spring.application.name=mini-erp
server.port=${PORT:8080}

# spring.datasource.url=${DB_URL:jdbc:postgresql://db.cmujicnnhkbmdufzvpfe.supabase.co:5432/postgres?sslmode=require}
# spring.datasource.username=${DB_USERNAME:postgres}
# spring.datasource.password=${DB_PASSWORD:4SYCfLe$,2MKbi!}
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.time_zone=UTC
spring.sql.init.mode=always
spring.jpa.defer-datasource-initialization=true

app.jwt.secret=${JWT_SECRET:super-secreto-super-secreto-super-secreto-1234567890}
app.jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}

spring.jackson.serialization.WRITE_DATES_AS_TIMESTAMPS=false
